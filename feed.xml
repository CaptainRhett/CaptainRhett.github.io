<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://captainrhett.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://captainrhett.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-01T14:04:09+00:00</updated><id>https://captainrhett.github.io/feed.xml</id><title type="html">Wuhuang Yao</title><subtitle>Personal website of Wuhuang Yao. </subtitle><entry><title type="html">LSB 图像隐写程序设计</title><link href="https://captainrhett.github.io/blog/2022/lsb-steganography/" rel="alternate" type="text/html" title="LSB 图像隐写程序设计"/><published>2022-09-09T09:13:23+00:00</published><updated>2022-09-09T09:13:23+00:00</updated><id>https://captainrhett.github.io/blog/2022/lsb-steganography</id><content type="html" xml:base="https://captainrhett.github.io/blog/2022/lsb-steganography/"><![CDATA[<p>此为北京理工大学网安专业某学期的某次作业。</p> <h1 id="一项目背景">一、项目背景</h1> <h2 id="1-隐写术">1. 隐写术</h2> <p>隐写术是一门关于信息隐藏的技巧与科学。所谓信息隐藏，指的是不让除预期接收者之外的任何人知晓信息的传递事件或者信息的内容。</p> <h2 id="2-lsb-隐写术">2. LSB 隐写术</h2> <p>LSB 隐写术是一种图像隐写术技术，其中通过将每个像素的<strong>最低有效位</strong>替换为要隐藏的消息位来将消息隐藏在图像中。</p> <h2 id="3-实现原理">3. 实现原理</h2> <p>为了更好地理解，让我们将数字图像视为像素的二维阵列。每个像素包含取决于其类型和深度的值。使用最广泛的颜色模式 RGB 时，这些值的范围为 0-255。</p> <p><img src="https://img2022.cnblogs.com/blog/2966064/202209/2966064-20220909082237848-432441629.png" alt="RGB 像素示意图"/></p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/lsb-480.webp 480w,/assets/img/lsb-800.webp 800w,/assets/img/lsb-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/lsb.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> </div> </div> <p>可以使用 ASCII Table 将消息转换为十进制值，然后再转换为二进制。随后逐个迭代像素值，将它们转换为二进制，并将每个最低有效位替换为消息序列中的对应位。</p> <p>要解码编码图像，只需反转该过程：收集并存储每个像素的最后一位，然后将它们分成 8 位一组，并转换回 ASCII 字符，从而获取隐藏消息。</p> <h1 id="二项目目标">二、项目目标</h1> <h2 id="1-主要目标">1. 主要目标</h2> <p>编写 <strong>LSB 图像隐写程序</strong>，包括：<strong>加密程序</strong>和<strong>解密程序</strong>。</p> <h2 id="2-目标分解">2. 目标分解</h2> <ul> <li>实现文本信息加密到图像</li> <li>实现图像文件解密到文本</li> </ul> <h1 id="三技术选型">三、技术选型</h1> <h2 id="1-如何以二进制方式读写图像文件">1. 如何以二进制方式读写图像文件？</h2> <p>首先安装 Pillow 库。打开控制台后输入以下命令即可自动安装：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pip</span> <span class="n">install</span> <span class="n">pillow</span>
</code></pre></div></div> <p>然后读取图片：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">PIL</span> <span class="kn">import</span> <span class="n">Image</span>  <span class="c1"># 从 pillow 库（即 PIL）中导入 Image 类
</span>
<span class="n">img</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="nf">open</span><span class="p">(</span><span class="sh">"</span><span class="s">../xx.jpg</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># 读取图片存入变量 img 中
</span><span class="nf">print</span><span class="p">(</span><span class="n">img</span><span class="p">.</span><span class="nb">format</span><span class="p">)</span>  <span class="c1"># 输出图片格式 str
</span><span class="nf">print</span><span class="p">(</span><span class="n">img</span><span class="p">.</span><span class="n">size</span><span class="p">)</span>  <span class="c1"># 输出图片大小信息 tuple = (int, int)
</span></code></pre></div></div> <p>获取像素信息：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 像素载入
</span><span class="n">pix</span> <span class="o">=</span> <span class="n">img</span><span class="p">.</span><span class="nf">load</span><span class="p">()</span>
<span class="n">width</span> <span class="o">=</span> <span class="n">img</span><span class="p">.</span><span class="n">size</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>  <span class="c1"># .size 方法返回的是一个元组 tuple = (int, int)
</span><span class="n">height</span> <span class="o">=</span> <span class="n">img</span><span class="p">.</span><span class="n">size</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>

<span class="c1"># 获取像素点的 RGB 值
</span><span class="n">rgb_list</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># 创建一个数组存储 RGB 值
</span><span class="k">for</span> <span class="n">y</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">height</span><span class="p">):</span>  <span class="c1"># 遍历每一个像素点，将图像看作二维数组
</span>    <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">width</span><span class="p">):</span>  <span class="c1"># 如果 x 循环在外层，输出的图像会发生九十度翻转
</span>        <span class="n">r</span><span class="p">,</span> <span class="n">g</span><span class="p">,</span> <span class="n">b</span> <span class="o">=</span> <span class="n">pix</span><span class="p">[</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">]</span>
        <span class="n">rgb_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">r</span><span class="p">)</span>
        <span class="n">rgb_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">g</span><span class="p">)</span>
        <span class="n">rgb_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">b</span><span class="p">)</span>
</code></pre></div></div> <p>输出图像：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 输出图像
</span><span class="n">j</span> <span class="o">=</span> <span class="mi">0</span>
<span class="n">pixels</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># 以 [(r1, g1, b1), (r2, g2, b2)] 形式存放每个像素点的 RGB 值，用于绘制图像
</span><span class="n">img_out</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="nf">new</span><span class="p">(</span><span class="n">img</span><span class="p">.</span><span class="n">mode</span><span class="p">,</span> <span class="n">img</span><span class="p">.</span><span class="n">size</span><span class="p">)</span>  <span class="c1"># 生成新图像，以原图的格式和大小
</span>
<span class="c1"># img_out 此时还是一张白纸，下面的代码用于更新 img_out 的像素信息
</span><span class="k">while</span> <span class="n">j</span> <span class="o">&lt;</span> <span class="nf">len</span><span class="p">(</span><span class="n">rgb_list</span><span class="p">):</span>
    <span class="n">pixels</span><span class="p">.</span><span class="nf">append</span><span class="p">((</span><span class="n">rgb_list</span><span class="p">[</span><span class="n">j</span><span class="p">],</span> <span class="n">rgb_list</span><span class="p">[</span><span class="n">j</span> <span class="o">+</span> <span class="mi">1</span><span class="p">],</span> <span class="n">rgb_list</span><span class="p">[</span><span class="n">j</span> <span class="o">+</span> <span class="mi">2</span><span class="p">]))</span>
    <span class="n">j</span> <span class="o">+=</span> <span class="mi">3</span>

<span class="n">img_out</span><span class="p">.</span><span class="nf">putdata</span><span class="p">(</span><span class="n">pixels</span><span class="p">)</span>  <span class="c1"># 放置像素信息
</span><span class="n">img_out</span><span class="p">.</span><span class="nf">save</span><span class="p">(</span><span class="sh">"</span><span class="s">img_out2.png</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># 将图像保存到根目录
</span></code></pre></div></div> <h2 id="2-信息如何转换">2. 信息如何转换？</h2> <p>二进制转文本（解密）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">bina_to_txt</span><span class="p">(</span><span class="n">bina</span><span class="p">):</span>
    <span class="c1"># 只要传入一个由二进制数组成的序列即可翻译成文本
</span>    <span class="n">tex</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">bina</span><span class="p">:</span>
        <span class="n">tex</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">chr</span><span class="p">(</span><span class="nf">int</span><span class="p">(</span><span class="n">i</span><span class="p">,</span> <span class="mi">2</span><span class="p">)))</span>
    <span class="k">return</span> <span class="n">tex</span>  <span class="c1"># 返回一个单字符序列
</span>

<span class="c1"># 要求 bina 的格式为 ["01010101", "11111111"]
</span></code></pre></div></div> <p>文本转二进制（加密）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">txt_to_bina</span><span class="p">(</span><span class="n">txt</span><span class="p">):</span>
    <span class="n">c</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">a</span> <span class="ow">in</span> <span class="n">txt</span><span class="p">:</span>
        <span class="n">c</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="sh">"</span><span class="s">{:0&gt;8}</span><span class="sh">"</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="nf">bin</span><span class="p">(</span><span class="nf">ord</span><span class="p">(</span><span class="n">a</span><span class="p">)).</span><span class="nf">lstrip</span><span class="p">(</span><span class="sh">"</span><span class="s">0b</span><span class="sh">"</span><span class="p">)))</span>
        <span class="c1"># 格式化并将二进制码保存起来
</span>        <span class="c1"># 注意要在左侧补齐八位，否则信息会错位
</span>
    <span class="n">resultlist</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">c</span><span class="p">:</span>
        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">8</span><span class="p">):</span>
            <span class="n">resultlist</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">i</span><span class="p">[</span><span class="n">j</span><span class="p">])</span>
    <span class="k">return</span> <span class="n">resultlist</span>


<span class="c1"># txt 为字符串类型，如 "hello world!"
# print(txt_to_bina("h")) 输出测试
# test_output: ["0", "1", "1", "0", "1", "0", "0", "0"]
</span></code></pre></div></div> <p>替换信息位（加密）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 替换信息位
</span><span class="n">i</span> <span class="o">=</span> <span class="mi">0</span>
<span class="k">while</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="nf">len</span><span class="p">(</span><span class="nf">txt_to_bina</span><span class="p">(</span><span class="n">txt</span><span class="p">)):</span>
    <span class="n">temp</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">bin</span><span class="p">(</span><span class="n">rgb_list</span><span class="p">[</span><span class="n">i</span><span class="p">]))</span>  <span class="c1"># 用 bin() 强制转换，bin() 返回字符串类型
</span>    <span class="n">temp</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="nf">txt_to_bina</span><span class="p">(</span><span class="n">txt</span><span class="p">)[</span><span class="n">i</span><span class="p">]</span>  <span class="c1"># 将 RGB 信息二进制码的最后一位替换成文本二进制码
</span>    <span class="n">rgb_list</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="sh">""</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">temp</span><span class="p">),</span> <span class="mi">2</span><span class="p">)</span>
    <span class="n">i</span> <span class="o">+=</span> <span class="mi">1</span>

<span class="c1"># txt_to_bina() 是自定义函数，旨在将文本转化成二进制码，返回一个单字符序列
# 这里直接用第一个像素的 RGB 值作为隐写的开头，所以 rgb_list 和 txt_to_bina() 的 index 是一样的
# 此处可以继续做加密处理
</span>
<span class="c1"># 特别注意：在 Python 中字符串不能直接修改，replace 方法不会改变原来的 string
# 修改字符串要将字符串转换成序列，修改序列后再将序列转成字符串，实现代码如下：
</span><span class="n">s</span> <span class="o">=</span> <span class="sh">"</span><span class="s">abcde</span><span class="sh">"</span>
<span class="n">temp</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>
<span class="n">temp</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">f</span><span class="sh">"</span>  <span class="c1"># 假设要将 s 的最后一位 "e" 修改为 "f"
</span><span class="n">s</span> <span class="o">=</span> <span class="sh">""</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">temp</span><span class="p">)</span>
</code></pre></div></div> <p>提取信息位信息：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 这里直接用的是 "hello world!" 的长度，后期优化可以加个旗帜识别
</span><span class="n">c</span> <span class="o">=</span> <span class="sh">""</span>
<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">96</span><span class="p">):</span>
    <span class="n">c</span> <span class="o">+=</span> <span class="nf">bin</span><span class="p">(</span><span class="n">rgb_list</span><span class="p">[</span><span class="n">i</span><span class="p">])[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>  <span class="c1"># 图像处理后得到 rgb_list，取二进制码的最后一位
</span>
<span class="n">out_list_bin</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">12</span><span class="p">):</span>
    <span class="n">out_list_bin</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">c</span><span class="p">[</span><span class="n">i</span> <span class="o">*</span> <span class="mi">8</span> <span class="p">:</span> <span class="p">(</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="mi">8</span><span class="p">])</span>  <span class="c1"># 每八位为一组转换出文本
</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">""</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="nf">bina_to_txt</span><span class="p">(</span><span class="n">out_list_bin</span><span class="p">)))</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="assignments"/><category term="python"/><category term="image-processing"/><category term="steganography"/><summary type="html"><![CDATA[北京理工大学课程作业：使用 Python 和 Pillow 实现 LSB 图像隐写的加密与解密。]]></summary></entry></feed>